FROM node:22-bookworm-slim

WORKDIR /workspace/small-web-tools

COPY --chown=node:node package.json package-lock.json ./
RUN npm install --global npm@10.9.2 \
    && npm ci \
    && npm cache clean --force

USER node

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
